# ES Class 初始化过程、原型链与对象层级关系

## 一、ES6 Class 的本质

ES6 的 `class` 本质上是**构造函数 + 原型链**的语法糖。理解 class，必须先理解它背后的原型机制。

```js
class Person {
  constructor(name) {
    this.name = name;
  }
  sayHi() {
    console.log(`Hi, I'm ${this.name}`);
  }
}
```

等价于：

```js
function Person(name) {
  this.name = name;
}
Person.prototype.sayHi = function() {
  console.log(`Hi, I'm ${this.name}`);
};
```

---

## 二、`new` 关键字做了什么（类初始化过程）

当你执行 `const p = new Person('Alice')` 时，引擎内部做了 **4 件事**：

```
1. 创建一个全新的空对象 {}
2. 将这个空对象的 [[Prototype]]（即 __proto__）链接到 Person.prototype
3. 以这个新对象为 this 上下文，执行 constructor 函数
4. 如果 constructor 返回了一个对象，则用该对象替代；否则返回步骤1创建的新对象
```

用伪代码表示：

```js
function myNew(Constructor, ...args) {
  // 步骤1：创建空对象
  const obj = {};
  // 步骤2：链接原型
  Object.setPrototypeOf(obj, Constructor.prototype);
  // 步骤3：执行构造函数，绑定 this
  const result = Constructor.apply(obj, args);
  // 步骤4：判断返回值
  return (result !== null && typeof result === 'object') ? result : obj;
}
```

---

## 三、原型链的核心关系

### 3.1 三个关键指针

| 指针 | 位置 | 指向 |
|------|------|------|
| `obj.__proto__` | 实例对象上 | 构造函数的 `prototype` |
| `Constructor.prototype` | 构造函数上 | 原型对象 |
| `Constructor.prototype.constructor` | 原型对象上 | 指回构造函数本身 |

### 3.2 关系图

```
                    ┌──────────────────────────┐
                    │    Person.prototype       │
                    │  ┌────────────────────┐   │
                    │  │ constructor: Person │   │
                    │  │ sayHi: function     │   │
                    │  └────────────────────┘   │
                    └──────────┬───────────────┘
                               │
              ▲                │ __proto__ 指向
              │                ▼
    ┌─────────┴──────┐   ┌──────────────┐
    │  constructor   │   │   实例 p      │
    │  Person        │   │  name: Alice │
    │  prototype ────┼──▶│  __proto__ ──┼──▶ Person.prototype
    └────────────────┘   └──────────────┘
```

用代码验证：

```js
const p = new Person('Alice');

p.__proto__ === Person.prototype;           // true
Person.prototype.constructor === Person;    // true
p.constructor === Person;                   // true（沿原型链找到）
```

---

## 四、完整的对象层级关系

### 4.1 单层类的层级

```
Object.prototype          ← 万物之源（顶层原型）
    ▲
    │ __proto__
    │
Person.prototype          ← 类的原型对象
    ▲
    │ __proto__
    │
实例 p                    ← new 出来的对象
```

**属性查找过程**：当访问 `p.sayHi()` 时：
1. 先看 `p` 自身有没有 `sayHi` → 没有
2. 沿 `p.__proto__` 到 `Person.prototype` → 找到了！执行它
3. 如果还没找到，继续到 `Object.prototype`
4. 最终到 `null`，返回 `undefined`

### 4.2 继承时的完整层级（重点）

```js
class Animal {
  constructor(name) { this.name = name; }
  eat() { console.log(`${this.name} is eating`); }
}

class Dog extends Animal {
  constructor(name, breed) {
    super(name);        // 调用父类构造函数
    this.breed = breed;
  }
  bark() { console.log('Woof!'); }
}

const dog = new Dog('Rex', 'Husky');
```

**`extends` 做了两件事**：

```
① 实例层面：Dog.prototype.__proto__ = Animal.prototype
   → 让实例能访问父类原型上的方法

② 静态层面：Dog.__proto__ = Animal
   → 让子类能访问父类的静态方法
```

完整原型链图：

```
Object.prototype
    ▲
    │
Animal.prototype          (eat方法在这里)
    ▲
    │
Dog.prototype             (bark方法在这里)
    ▲
    │
实例 dog                  (name, breed在这里)
```

```
Function.prototype
    ▲
    │
Animal                   (静态方法在这里)
    ▲
    │
Dog                      (静态方法在这里)
```

代码验证：

```js
// 实例原型链
dog.__proto__ === Dog.prototype;                    // true
Dog.prototype.__proto__ === Animal.prototype;       // true
Animal.prototype.__proto__ === Object.prototype;    // true
Object.prototype.__proto__ === null;                // true

// 静态（构造函数）原型链
Dog.__proto__ === Animal;                           // true
Animal.__proto__ === Function.prototype;            // true
```

---

## 五、属性查找的完整路径

当执行 `dog.eat()` 时，引擎的查找路径：

```
dog 自身 → Dog.prototype → Animal.prototype → Object.prototype → null
   ❌           ❌              ✅ 找到！
```

当执行 `dog.toString()` 时：

```
dog 自身 → Dog.prototype → Animal.prototype → Object.prototype → null
   ❌           ❌              ❌                ✅ 找到！
```

---

## 六、总结

| 概念 | 要点 |
|------|------|
| **class 本质** | 构造函数 + prototype 的语法糖 |
| **new 做了什么** | 创建空对象 → 链接原型 → 执行 constructor → 返回对象 |
| **`__proto__`** | 实例指向其构造函数的 `prototype` |
| **原型链查找** | 沿 `__proto__` 逐级向上，直到 `null` |
| **extends 继承** | 同时链接实例原型链和静态原型链 |
| **顶层** | 所有原型链最终指向 `Object.prototype`，其 `__proto__` 为 `null` |

核心一句话：**原型链就是 JavaScript 实现继承的机制，`__proto__` 是串联这条链的线索，而 `prototype` 是构造函数用来给未来实例提供共享方法的"模板"。**

---
原型链理解到位后，通常还会遇到 `super` 关键字和 `this` 绑定的问题，需要我展开讲讲这两块吗？